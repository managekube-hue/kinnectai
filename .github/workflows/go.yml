name: Go CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  structure:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Verify monorepo structure
      shell: pwsh
      run: ./scripts/ci/verify-monorepo-structure.ps1

  test:
    needs: structure
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22'
    - run: cd services/go/feed-service && go mod download
    - run: cd services/go/feed-service && go test ./...

  build:
    needs: structure
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22'
    - run: cd services/go/feed-service && go build ./cmd/api