targets:
  $default:
    builders:
      drift_dev|drift_dev:
        enabled: false
      json_serializable|json_serializable:
        generate_for:
          - lib/models/*.dart
          - lib/models/dtos/*.dart
      freezed|freezed:
        generate_for:
          - lib/models/*.dart
          - lib/models/dtos/*.dart
