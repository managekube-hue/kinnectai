import 'package:flutter/material.dart';

class RootProfileScreen extends StatelessWidget {
  const RootProfileScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}