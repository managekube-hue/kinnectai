# Kernel Service

A placeholder Python service for KinnectAI kernel inference.

## Local development

```bash
cd services/python/kernel-service
pip install -r requirements.txt
python main.py
```

## Docker

```bash
docker build -t kinnectai/kernel-service ./services/python/kernel-service
```
